package instuments.classes.electro;

import instuments.classes.Type;
import instuments.instruments.ElectroAbstract;

public class BassGuitar extends ElectroAbstract {

    protected BassGuitar(String instrumentName, double price, Type type) {
        super(instrumentName, price, type);
    }
}
