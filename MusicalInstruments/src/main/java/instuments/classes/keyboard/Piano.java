package instuments.classes.keyboard;

import instuments.classes.Type;
import instuments.instruments.KeyboardAbstarct;

public class Piano extends KeyboardAbstarct {

    public Piano(String instrumentName, double price, Type type) {
        super(instrumentName, price, type);
    }
}
