package instuments.classes.keyboard;

import instuments.classes.Type;
import instuments.instruments.DrumAbstract;

public class Аccordion extends DrumAbstract {

    public Аccordion(String instrumentName, double price, Type type) {
        super(instrumentName, price,type);
    }
}
