package instuments.classes.strings;

import instuments.classes.Type;
import instuments.instruments.StringAbstract;

public class Guitar extends StringAbstract {
    public Guitar(String instrumentName, double price, Type type) {
        super(instrumentName, price,type);
    }
}
